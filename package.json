{
  "name": "bidflow",
  "private": true,
  "version": "0.1.0",
  "workspaces": [
    "apps/*"
  ],
  "scripts": {
    "dev:backend": "npm run dev -w @bidflow/backend",
    "build:backend": "npm run build -w @bidflow/backend"
  }
}
